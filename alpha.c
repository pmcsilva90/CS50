#include <stdio.h>
#include <cs50.h>

int main(void)
{
    string word = get_string("Word: ")

    int i;
    while (word[i] > 96)
    {
        if (word[i] > word[i + 1])
        {
        printf("False");
        }
        else
        printf("True")
    }
}
