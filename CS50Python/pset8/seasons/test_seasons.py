import seasons
import pytest

def main():
    test_dateformat()
    test_valid_date()


def test_dateformat():
    with pytest.raises(SystemExit)
    # assert validate_dob("") ==
    # assert validate_dob("") ==
